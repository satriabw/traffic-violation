[project]
name = "detection-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "shared",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["detection_worker*"]
