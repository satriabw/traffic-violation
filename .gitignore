.venv/
__pycache__/
*.pyc
.pytest_cache/
data/
*.sqlite
*.sqlite-wal
*.sqlite-shm
.DS_Store
*.egg-info/
# setuptools leaves this behind on a non-editable `pip install ./packages/...`
build/
traffic-violation-lld-reference.md
.env
*.onnx
# Local-only fix for networks that block outbound UDP/53 (Docker's build resolver
# can't fall back to TCP the way the host can). See docker/README or ask before
# assuming this is needed elsewhere.
docker-compose.override.yml
research/*